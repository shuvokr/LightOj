#include <stdio.h>
#include <string.h>

int DP(int s, int e);

int mamo[ 110 ][ 110 ];
char in[ 110 ];

int main()
{
    int T, kag = 0;
    scanf("%d", &T); getchar();
    while( T-- )
    {
        scanf("")
    }
    return 0;
}
